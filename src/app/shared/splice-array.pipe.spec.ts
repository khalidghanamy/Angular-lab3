import { SpliceArrayPipe } from './splice-array.pipe';

describe('SpliceArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SpliceArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
